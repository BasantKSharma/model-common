package com.gogo.model.common.domain.constants;

/**
 * Enum for order status
 * */
public enum OrderStatus {

    NEW, CANCELLED, PENDING_PAYMENT, PROCESSING, PACKAGED, PICKED, SHIPPED,
    DELIVERED, RETURNED, PAID, REFUNDED
}
